#context "#reading_time method" do
    #it "returns an estimate amount of time neede to read in minutes for a given text" do
      #diary.add(entry_1)
      #expect(diary.reading_time(1)).to eq 5
    #end

    #it "returns 2" do
      #diary.add(entry_1)
      #diary.add(entry_2)
      #expect(diary.reading_time(5)).to eq 2
    #end
  #end